# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.idea
*.vscode
posr.db*
.cursor
cert
posr.db
posr.db1

database/clog
database/manifest
database/sstables
database/vlog
database/wal
database/LOCK

.orchids

# End-user guide generated artifacts (MD/HTML/PDF) and Playwright output
docs/user-guide/dist
docs-automation/.output
docs-automation/test-results
docs-automation/playwright-report

# Print service persisted logo (written at runtime)
printing/data
# API AI usage counters (written at runtime)
api/data
backups/surrealdb
.qwen
# Real env files carry secrets (SURREAL_USER/PASS, GATEWAY_JWT_SECRET, API keys).
# Never commit them. Only the .env.example templates are tracked.
.env
.env.local
api/.env
gateway/.env
payments/.env
migrations/scripts/node_modules/
migrations/scripts/package.json
migrations/scripts/package-lock.json

# Fiscal certificates (test certs OK to commit, production certs NEVER)
certs/**/*.p12
certs/**/*.pfx
certs/**/*.key
certs/**/production*

